export function handleVocabularies(
  req: Request,
  options: { debug: boolean; htdocs: string },
): Response {
  console.log("FIXME: handleVocabularies() not implemented");
  const body = "<html>handleVocabularies Not implemented</html>";
  return new Response(body, { status: 501 });
}
